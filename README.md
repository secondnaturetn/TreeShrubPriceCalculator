# Tree/Shrub Per App Price Calculator

Static Azure web app for the technician calculator.

## Azure Static Web Apps settings

- App location: `/`
- API location: leave blank
- Output location: leave blank
- Build preset: `Custom`

The app is a single static page at `index.html`, so no build command is required.
